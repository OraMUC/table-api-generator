{
  "name": "om_tapigen",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/OraMUC/table-api-generator.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "npx ploc --in src/OM_TAPIGEN.pks --out README.md && npx markdown-toc -i --maxdepth 2 docs/parameters.md && npx markdown-toc -i --maxdepth 2 docs/getting-started.md && npx markdown-toc -i --maxdepth 2 docs/changelog.md",
    "build": "node src/build.js",
    "postbuild": "echo exit | sqlplus -S tests/oracle@localhost:1521/xepdb1 @om_tapigen_install.sql",
    "watch": "chokidar src/*.pks src/*.pkb src/om_tapigen_install.sql --initial -c \"npm run build\""
  },
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "markdown-toc": "^1.2.0",
    "ploc": "^0.6.1"
  }
}
